GiL(list):
init: greatest := -1;
      goto loop;

loop: if list = '() goto end else cont;

cont: term := hd(hd(list));
      list := tl(list);
      if (greatest <= term) goto update else loop;

update: greatest := term;
        goto loop;


end: return greatest;